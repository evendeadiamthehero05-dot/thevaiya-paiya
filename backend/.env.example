# Backend Environment Variables

# Node environment: 'development' for local testing, 'production' for deployment
NODE_ENV=production

# Server port (default 3001)
PORT=3001

# Frontend URL (optional - CORS auto-detects localhost and Netlify)
# For custom frontend URLs, set explicitly:
# FRONTEND_URL=https://your-site-name.netlify.app

# Database: Uses SQLite locally, file created at ./game.db
